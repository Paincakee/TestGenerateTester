models:
  Like:
    post_id: id foreign:posts
    user_id: id foreign:users
    relationships:
      belongsTo: Post, User
    indexes:
      - unique: post_id,user_id

controllers:
  Like:
    resource: api
