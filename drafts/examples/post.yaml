models:
  Post:
    title: string
    body: text nullable
    status: enum:draft,planned,published,archived default:draft
    user_id: id foreign:users
    published_at: timestamp nullable
    relationships:
      belongsTo: User

controllers:
  Post:
    resource: api
